para llamar al helper

public GrupNot(Usuario u)
{
    this.u = u;
    InitializeComponent();
    MaterialSkinHelper.ApplyMaterialSkin(this);
    menu_lbl.Text = "Aquí están tus grupos " + u.usuario;
    rutaArchivoJson = @"C:\Users\nopes\OneDrive\Escritorio\...\GruposUsuario\" + u.usuario + ".json";
}




-------------------------
refactorizar para la base de datos en usuario:
usuario.cs

Datos:
--- usuario ----- 
id -int 
rol - Admin/Usuario
usuario -usuario
password -password nota: se usa al insertar sha2(@pass, 256) en el backend, no es necesaria en la base de datos.
nombre -string
correo -string
id_admin -int -noa: aqui no se ha visto donde se coloca el id del admin, poner null o default.

-------------------------
CREATE TABLE usuario (
    id INT AUTO_INCREMENT PRIMARY KEY,
    rol ENUM('Admin', 'Usuario') NOT NULL,
    usuario VARCHAR(50) NOT NULL UNIQUE,
    nombre VARCHAR(100) NOT NULL,
    correo VARCHAR(100) NOT NULL UNIQUE,
    id_admin INT DEFAULT NULL,
    CONSTRAINT fk_admin FOREIGN KEY (id_admin) REFERENCES usuario(id)
);

---- grupo -------
id -int 
numero_usuarios - int
id_admin - int
nombre -string
descripcion -string

--- notificacion -----
id - int
tipo -string
remitente -string
receptor -string 
descripcion -string
fecha -string
prioridad -prioridad


se esta usando en la clase usuarios:
	en notificacion.cs
	usuarios.cs (para operaciones e administrador)
	usuarios_views
	registro_view
	menu_admin
	menu_user
	grupNot
	grupo_mod
	grupo_notificacion
	userNot
	seccion_mod
	seccion_notificacion
	secNot
	
Notas:
hacer la tabla (usuario)
al crear un usuario se usa sha, a la hora de insertar.

-----------------------------------------
se esta usando en la clase grupo:
	Grupo
	grupos_views
	menu_admin
	
------------------------------------
se esta usando en la clase notificacion
	Notificacion
	notificaciones_view
	Menu_Admin
	Menu_user
	grupo_notificacion
	UserNot
	

Haciendo: Factorizando usuarios
Hacer despues: refactorizar notificacion y grupo

errores: 
Se esta registrando admins en ves de usuarios.


Cambios: 
Ya registra usuario.
inicia y registra bien usuarios.

usuarioJuan
juan123

Amanda67
amandA123@
